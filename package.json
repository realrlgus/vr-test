{
  "dependencies": {
    "gh-pages": "^2.2.0"
  },
  "homepage": "https://realrlgus.github.io/vr-test",
  "scripts": {
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
